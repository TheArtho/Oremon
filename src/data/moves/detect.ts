import { MoveData } from "../../types/MoveData";

const detect: MoveData = {
  id: "detect",
  name: "oremon.detect.name",
  description: "oremon.detect.description",
  category: "status",
  accuracy: undefined,
  power: undefined,
  pp: 5,
  priority: 4,
  flags: {},
  target: "user",
  type: "fighting",
  effectId: "none",
  effectChance: undefined
};

export default detect;
